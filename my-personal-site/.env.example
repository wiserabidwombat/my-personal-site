# Copy to .env.local (already gitignored) and fill in real values.
# NOTION_TOKEN / NOTION_DATA_SOURCE_ID are also required as env vars in the
# Vercel dashboard for api/games.ts -- never commit real values here.
NOTION_TOKEN=
NOTION_DATABASE_ID=
NOTION_DATA_SOURCE_ID=
